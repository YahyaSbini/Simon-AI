DATABASE_URL=postgres://postgres:postgres@localhost:5432/simon
BETTER_AUTH_SECRET=generate-with-openssl-rand-base64-32
BETTER_AUTH_URL=http://localhost:3000

# Google OAuth client with the Calendar API enabled. Optional: without it,
# Google sign-in and calendar sync stay hidden.
# Redirect URI: <BETTER_AUTH_URL>/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
